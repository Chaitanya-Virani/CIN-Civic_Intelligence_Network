import type { ModuleManifest } from "../types";

export const manifest: ModuleManifest = {
  id: "elections",
  label: "Elections",
  blurb: "Slate-based representative elections.",
  kind: "page",
  status: "preview",
  nav: { order: 7, icon: "Vote" },
};
