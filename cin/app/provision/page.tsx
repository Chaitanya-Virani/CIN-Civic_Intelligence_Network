import { ProvisionClient } from "./provision-client";

export const metadata = {
  title: "Provision Tenant — CIN Civic Intelligence Network",
};

export default function ProvisionPage() {
  return <ProvisionClient />;
}
