import type { ModuleManifest } from "../types";

export const manifest: ModuleManifest = {
  id: "noticeboard",
  label: "Notice Board",
  blurb: "Official announcements and gazettes.",
  kind: "page",
  status: "preview",
  nav: { order: 9, icon: "Megaphone" },
};
