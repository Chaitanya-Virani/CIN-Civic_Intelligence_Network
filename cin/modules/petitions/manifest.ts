import type { ModuleManifest } from "../types";

export const manifest: ModuleManifest = {
  id: "petitions",
  label: "Petitions",
  blurb: "Threshold-based petitions that escalate automatically.",
  kind: "page",
  status: "preview",
  nav: { order: 5, icon: "ScrollText" },
};
